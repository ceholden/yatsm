# By default just install the minimum requirements from "common"
# Install requirements with pip as:
#   pip install -r requirements.txt
# Install requirements with conda as:
#   conda create -n <name> --file requirements.txt
-r requirements/common.txt
